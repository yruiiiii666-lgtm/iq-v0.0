# 场景地点、频谱与 IQ 数据统一关联表填写说明

关联表为 UTF-8 编码的 CSV 文件，可以直接用 Excel 打开和填写。

## 字段格式

| 列名 | 是否必填 | 填写规则 |
| --- | --- | --- |
| 序号 | 是 | 地点编号；同一地点关联多组 IQ 时使用相同序号 |
| 城市 | 是 | 必须与频谱数据目录一致，例如 `北京测点`、`长春测点` |
| 地点 | 是 | 建议与频谱数据目录中的测点名称完全一致 |
| 场景类型 | 否 | 该字段同时决定频谱和 IQ 数据的场景分类；留空时软件根据地点名称建议 |
| 频谱数据目录名称 | 是 | 对应原始频谱根目录下的测点文件夹名称，可与显示地点名称不同 |
| IQ数据相对目录 | 否 | IQ文件相对于界面所选“IQ根目录”的子目录；文件就在根目录时留空 |
| IQ数据组名称 | 否 | 可填写完整主文件名，也可填写同一地点所有IQ文件的公共前缀 |
| 备注 | 否 | 可填写采集日期、关键频点、道路状态等说明 |

## 填写规则

1. 一个地点只关联一个 IQ 数据组时，填写一行。
2. 同一地点的所有IQ文件具有共同前缀时，只需填写一行，软件会自动展开全部频点。
3. IQ 设备的一组数据由同名的 `.wsm`、`.ws1`、`.ws2` 组成；只有三个文件都存在时才会建立关联。
4. IQ相关字段全部留空时，只建立频谱地点分类，不建立 IQ 关联。
5. 可以在表格末尾新增地点。新增地点只有在频谱特征库中存在同名城市和地点时才能显示对应频谱；IQ 关联不受此限制。
6. 场景类型以关联表为准。软件保留参考标准中的场景选项，并合并表中新增的自定义场景类型。
7. 导入不会移动、改名或修改原始频谱和 IQ 文件，只会更新 `spectrum_feature_library.db` 中的元数据关系。

## 示例

```csv
序号,城市,地点,场景类型,频谱数据目录名称,IQ数据相对目录,IQ数据组名称,备注
1,北京测点,朝新嘉园东里四区,居民区,朝新嘉园,,20260611beijingchaoxinjy,自动关联该地点全部频点
2,长春测点,五二三电台,电台,11电台523,,,暂未采集IQ
```

例如IQ根目录中存在 `20260611beijingchaoxinjy95m`、`20260611beijingchaoxinjy775m`、`20260611beijingchaoxinjy875m` 等三文件组时，表中只写公共前缀 `20260611beijingchaoxinjy`，软件会一次性关联全部完整数据组。
